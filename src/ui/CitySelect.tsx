import React from 'react';

const CitySelect = () => {
	return (
		<select
			className="p-2.5 mx-4 bg-zinc-100 rounded-lg border-2 border-black"
		>
			<option value="">All Cities</option>
		</select>
	);
};

export default CitySelect;
